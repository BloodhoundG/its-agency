import type { ProductFilters } from '../../types/types.ts';

export const activeFilters: ProductFilters = {
    inStock: false,
    isContract: false,
    isExclusive: false,
    isNew: false,
    onSale: false,
};
